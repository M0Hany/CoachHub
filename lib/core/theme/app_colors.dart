import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF0D122A);  // Dark Navy
  static const accent = Color(0xFF0FF789);   // Green Accent
  static const background = Color(0xFFDDD6D6); // Light Gray
  static const textLight = Colors.white;
  static const textDark = Color(0xFF0D122A);
  static const inputBackground = Colors.white24;
  static const shadowDark = Colors.black;
  
  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFF44336);
  static const warning = Color(0xFFFFC107);
  static const info = Color(0xFF2196F3);
} 